import * as types from '../actions/actionTypes';

export default function routeReducer(state = {route:'login',routeProps:{}}, action) {
  switch (action.type) {
    default:
      return state;
  }
}
